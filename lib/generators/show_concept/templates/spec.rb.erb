# frozen_string_literal: true

describe <%= class_name %>::Cell::Show, type: :cell do
  subject { described_class.(model, current_user: user) }

  controller <%= class_name.pluralize %>Controller

  let(:user) { Admin.new }
  let(:model) { build_stubbed(:<%= class_name.split("::").last.underscore %>) }
end
