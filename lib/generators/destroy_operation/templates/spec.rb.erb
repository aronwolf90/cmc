# frozen_string_literal: true

require "rails_helper"
require_relative "../../shared_examples/standard_create_operation"

describe <%= class_name %>DestroyOperation do
  let(:model) { <%= class_name.split("::").last %>.new }

  it_should_behave_like "standard destroy operation"
end
